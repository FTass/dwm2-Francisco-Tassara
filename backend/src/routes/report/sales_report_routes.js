const { Router } = require('express');
const router = Router();

router.get('/', );             
router.get('/:id', );          
router.post('/', );             
router.put('/:id', );          
router.delete('/:id', );        


router.get('/:id/pdf', );
router.get('/:id/summary', );

module.exports = router;
